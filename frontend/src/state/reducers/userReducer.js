import * as types from '../constants/actionTypes';

const initialState = {
  currentUser: {},
  lastSearchResults: [],
  lastSearch: {},
};

//TODO build out current user reducer
const currentUserReducer = (state = initialState, action) => {};
